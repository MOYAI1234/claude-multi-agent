# 多模型路由规则

收到任务时，主动判断执行者，无需征求用户同意，执行后告知即可。

## 交给 `/codex:rescue` 的条件（满足任意一条）
- 需要修改代码文件
- 需要跨多个文件理解上下文
- 我自己尝试超过一轮仍未解决的代码问题
- 用户明确说"让 Codex 处理"

## 交给 `/trae:run` 的条件（满足任意一条）
- 图片识别、OCR 类任务
- 输入输出格式完全固定的抽取/转换任务
- 执行已有 skill 且不需要额外推理
- 用户明确说"用豆包"

## 我自己处理的条件
- 需要理解意图、做判断、权衡方案
- 报告撰写、文风、洞察、归因
- 规划、拆解、设计类任务
- 不满足上面两条的其他情况

## 路由行为规范
- 路由前不需要征求用户同意，直接执行
- 路由后简短告知："已交给 Codex / 豆包处理"
- 同一任务不重复路由
